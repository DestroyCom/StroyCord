---
"stroycord": patch
---

Fix missing i18n translation paths for voiceChannelNotFound and related keys (moved under errors.arguments to match slash command references). Add python3 to Docker production image so yt-dlp can execute. Silence message.delete() permission errors when bot lacks MANAGE_MESSAGES.
